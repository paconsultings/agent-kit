id: devkit-deploy
name: Devkit Deploy
version: 0.1.0
agents: [codex, claude]
description: Deploy artifacts to SiMa DevKit devices.
compatibility:
  env_types: [devkit]
  env_subtypes: [modalix, davinci]
  min_cli_version: 2.1.1
runtime:
  requires:
    - ssh
    - rsync
