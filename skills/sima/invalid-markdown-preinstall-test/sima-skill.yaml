id: invalid-markdown-preinstall-test
name: Invalid Markdown Preinstall Test
version: 0.1.0
agents: [codex, claude]
description: Deliberately invalid markdown fixture to verify preinstall validation failures.
compatibility:
  env_types: [host]
  env_subtypes: [mac, linux]
  min_cli_version: 2.1.1
runtime:
  requires:
    - python>=3.10
